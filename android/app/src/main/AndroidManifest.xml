<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- allowBackup=false: finansal veri (IndexedDB) cihazda şifresiz durur; adb backup
         veya otomatik bulut yedeğiyle cihaz dışına çıkmasın. Kullanıcının kendi yedeği için
         uygulama içi "Dışa Aktar" (JSON) ve QR ile cihazlar arası senkron zaten var. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- AdMob uygulama kimliği. Google Mobile Ads SDK bu meta-data olmadan
             açılışta uygulamayı çökertir. Değer AdMob konsolundaki App ID'dir. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9692001502823344~9074574037" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- QR okuma (cihaz eşleştirme) için WebView kamera erişimi.
         Capacitor'ın WebChromeClient'i getUserMedia çağrısında bu izni çalışma
         anında ister; izin manifest'te olmadan istem hiç görünmüyordu. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- required=false: kamera yalnızca opsiyonel QR senkron için; kamerasız
         cihazlar (bazı tabletler/Chrome OS) yine de uygulamayı kurabilsin. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>
